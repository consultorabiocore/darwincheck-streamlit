name: Test de mi App
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Ejecutar la aplicación o Tests
        run: streamlit run app.py

      - name: Ejecutar la aplicación o Tests
        run: python main.py
